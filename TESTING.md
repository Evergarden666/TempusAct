# TempusAct 验收记录

## 当前状态

更新日期：2026-09-09。1.1.0 已通过 `adb install -r --no-streaming` 从 1.0.0 覆盖升级到小米 15。设备报告型号 24129PN74C、Android 17（API 37）、HyperOS OS4.0.0.8.XOCCNXM；安装后确认 versionCode 2、versionName 1.1.0，首页名称为 TempusAct。

本文保留测试结论与适用边界，实际使用的方案名称、使用次数与时刻均已省略。真机截图、界面导出和原始测试日志仅保存在本地 `artifacts/`，不随源码上传。下文的手动验收配置是可重复使用的示例，不是个人使用记录。

| 检查 | 状态 |
| --- | --- |
| 时间状态机、提醒和历史分组单元测试（1.1.0） | 58 项全部通过，0 失败、0 跳过 |
| Release APK、签名、lint | 构建成功；APK 签名与 ZIP 对齐验证通过；lint 无问题 |
| SQLite、恢复、删除与迁移仪器测试（1.1.0） | 小米 15 上 17 项全部通过 |
| 界面和权限（1.0.0） | 已确认启动、计时和历史页面可见；通知与精确闹钟权限已取得 |
| 新版历史分组、删除及无损迁移 | 分组 16 项单元测试与删除、迁移仪器测试通过；真机确认升级后原有方案与历史保留，按方案分组正确，删除后汇总同步更新且方案保留 |
| 键盘布局 | 真机复查通过：保存按钮紧邻键盘上方，重复底部空白已消除 |
| 声音、震动、常亮、长时间锁屏、HyperOS 后台 | 尚未完成真机验收 |

单元测试覆盖事件和轮次边界、准备只执行一次、延迟刷新跨多个边界、暂停和恢复排除等待、提前结束、临时轮数和快照、时长格式，以及提醒去重和暂停边界提醒。单元测试不能替代 Android 服务及实际声音震动验收。

APK 可从 [v1.1.0 发布页面](https://github.com/Evergarden666/TempusAct/releases/tag/v1.1.0) 下载；本地构建产物为 `artifacts/TempusAct-1.1.0.apk`，校验和位于相邻 `.sha256` 文件。签名、版本、权限和对齐检查已通过，原始检查记录保存在本地。应用未声明网络权限。原生 aapt 在本机不能读取中文目录，因此元数据检查使用构建目录中的同一 APK，并核对其 SHA-256 与交付包完全一致。

新增自动化测试已验证同方案多条记录汇总、同名不同方案独立、方案改名与删除后历史保留、删除后重新汇总、待恢复记录不能被删除、删除后的旧完成回调不会复活记录，以及数据库从 v1 升至 v2 保留方案、历史、活动会话。删除测试使用独立数据库，不删除用户正式数据。

1.1.0 的应用名称已通过 APK 元数据验证为 TempusAct，版本为 1.1.0（versionCode 2），签名证书与原 1.0.0 完全一致。覆盖安装后，真机界面确认原有方案与历史保留，历史按来源方案正确分组。此项核对的是界面中的原有方案和记录，活动会话迁移另由仪器测试验证。

真机已确认历史删除后，总用时、记录数和分组同步更新，方案保持不变。单条和批量删除的存储行为均由仪器测试覆盖，本次未分别记录两种删除确认弹窗的真机流程。

键盘修复依据：[Compose Insets 的消费规则](https://developer.android.com/develop/ui/compose/system/insets-ui)。根布局应用 Scaffold 的 padding 后调用 consumeWindowInsets，防止 imePadding 再次预留相同键盘高度。修复后已在真机创建方案页聚焦名称输入框，确认保存按钮紧邻键盘上方；该检查没有保存测试方案。

## 构建与自动化测试

```powershell
.\scripts\build.ps1 -Tasks testDebugUnitTest,assembleRelease,assembleDebugAndroidTest,lintDebug
```

成功后查看本地生成的 `artifacts/test-report/index.html`、`artifacts/test-results`、`artifacts/lint-results-debug.html`；这些报告不随源码上传。`assembleDebugAndroidTest` 只编译测试安装包，不代表测试已经在手机运行。构建环境和签名恢复要求见 [README.md](README.md#构建)。

连接已开启 USB 调试的小米 15，确认手机授权电脑，之后运行：

```powershell
adb devices -l
.\scripts\build.ps1 -Tasks connectedDebugAndroidTest
```

仪器测试为每项测试使用独立临时数据库，不读写正式方案和历史。覆盖方案独立保存、历史快照、删除方案保留历史、重复完成去重、事务清理、旧回调不覆盖新任务、恢复历史合并与准备草稿规则，以及新增删除与升级场景。通过 Gradle 连接测试时，报告位于 `%TEMP%/jianshen-clock-app-build/reports/androidTests/connected`。

本次已安装测试 APK，直接执行 `adb shell am instrument -w -r -e class com.jianshen.clock.data.ClockRepositoryTest com.jianshen.clock.test/androidx.test.runner.AndroidJUnitRunner`；输出结尾为 `OK (17 tests)`，原始日志仅保存在本地。

## 小米 15 手动验收

先记录 Android 版本、HyperOS 版本、APK 版本，以及是否允许通知、后台运行和精确闹钟。每项记录通过/失败和观察到的情况。

| 步骤 | 应看到的结果 |
| --- | --- |
| 安装 APK，首次启动 | 正常打开创建方案页面，中文显示完整 |
| 创建“晨间训练”：运动 10 秒、休息 5 秒，默认 3 轮、准备 5 秒 | 保存后可在方案列表选择；预计 50 秒 |
| 再创建另一方案；退出重开 | 两套方案名称、事件、顺序、时长与轮数保留 |
| 选择晨间训练，将本次轮数改为 2 | 预计 35 秒，保存的默认值仍为 3 轮 |
| 开始本次 2 轮 | 准备 5 秒后运动→休息→运动→休息，完成后停止；准备只执行一次 |
| 观察切换及完成 | 每次切换一次提示音和震动，当前/下一事件正确；完成提醒与切换提醒可区分 |
| 查看本次历史 | 实际计时 30 秒、完成 2/2 轮、已完成，详情保存本次 2 轮 |
| 暂停约 20 秒后继续并完成 | 暂停时剩余时间冻结，实际事件时长仍为 30 秒 |
| 在准备末尾或事件末尾快速暂停，稍后继续 | 不丢事件、不重复轮次；尚未提醒的新事件在继续时提醒一次 |
| 准备中结束；事件阶段计时一部分后结束 | 前者无历史；后者保存实际计时及完整完成轮数，标注提前结束 |
| 运行一部分后“重新开始”，确认 | 旧记录保存，新计时从准备开始，沿用本次 2 轮 |
| 完成后“再来一次” | 新会话沿用本次 2 轮；回方案列表重新选择则恢复默认 3 轮 |
| 编辑或删除已使用的方案 | 历史名称、事件和当时轮数保持原内容 |
| 输入空名称、0 秒、0 轮或非数字 | 明确提示无效配置，无法启动 |
| 旋转屏幕、切换应用后返回 | 事件与轮次连续，不重头开始 |
| 前台运行超过系统息屏时间 | 保持常亮；暂停或完成后遵循系统自动息屏 |
| 手动锁屏、后台运行并跨越事件边界 | 倒计时持续，能收到声音和震动；通知可暂停、继续 |
| 关闭声音或震动后分别运行 | 对应提醒关闭，另一提醒仍按配置工作 |
| 拒绝通知后开始 | 应用仍能计时；允许通知后可看到计时通知 |
| 飞行模式下创建、运行、查看历史 | 无需账号或网络，数据正常保存 |
| 将事件名设为 80 字，放大系统字体 | 倒计时、轮次和操作仍可读；可滚动到操作区域 |

长时间锁屏另用“运动 30 秒→休息 10 秒，30 轮”运行约 20 分钟，记录实际边界提醒、完成时间、后台省电设置及有无系统中断。需要时可在调试设备上用 `adb shell dumpsys deviceidle force-idle` 测试 Doze，测试结束执行 `adb shell dumpsys deviceidle unforce` 恢复正常模式。

## 中断与覆盖升级

1. 开始计时并累计至少几秒事件时间，运行 `adb shell am force-stop com.jianshen.clock`，等待后重新打开应用。
2. 应显示等待恢复，停在最近保存的进度；进入历史可看到同一条待恢复记录，并能返回恢复页。
3. 继续后中断等待不加入实际时长，最终只产生一条记录；直接结束时标记已中断。
4. 在准备阶段重复中断流程，结束或重置不应生成事件历史。
5. 后续版本使用相同应用标识和原签名覆盖安装，确认已有方案、历史和恢复进度保留。1.0.0 → 1.1.0 已在真机核对方案和历史保留；活动会话的 v1 → v2 迁移已由仪器测试覆盖，尚未执行带有活动计时的真机覆盖升级流程。

系统强制停止会终止计时和提醒；恢复流程用于保留已保存进度，不应将这段停止时间当作后台连续计时成功。
