# TempusAct 安卓循环计时器开发计划

## 1. 需求理解

做一个在安卓手机上运行的循环计时器。用户先配置一组有顺序的事件，每个事件都有自定义名称和计时时长，再设置整组事件的循环次数。

用户首次使用时可以配置并命名一套循环方案，保存后反复使用。应用支持保存多套方案，后续打开时直接选择已有方案开始计时，不必每次重新填写事件、时长和循环次数。

选择方案后，还可以只调整本次的循环次数；该调整不修改已保存的方案，计时与历史记录均使用本次实际设置。

点击开始后，应用先执行方案设置的准备倒计时，再从第一个事件开始倒计时；关闭准备时间时直接开始第一个事件。当前事件结束，自动开始下一个事件；最后一个事件结束，表示完成一轮。如果还有剩余轮次，就从第一个事件重新开始；完成指定轮数后停止。

应用还需要保留历史记录，让用户查看在哪个日期使用了哪个循环方案，以及实际进行了多久的计时。历史记录属于第一版范围。

例如：

| 顺序 | 事件名称 | 时长 |
| --- | --- | --- |
| 1 | 热身 | 30 秒 |
| 2 | 运动 | 60 秒 |
| 3 | 休息 | 20 秒 |

设置循环 3 次，每轮 110 秒，事件计时总时长为 330 秒，即 5 分 30 秒。热身和休息也属于这组事件，每一轮都会执行。若使用默认的 5 秒开始前准备，则在没有暂停时整次预计需要 5 分 35 秒；这段准备只执行一次。

## 2. 用户已经明确的需求

- 能在安卓手机上运行。
- 用户可以配置多个事件，并自己命名。
- 每个事件可以单独设置计时时长。
- 所有事件按指定顺序执行。
- 全部事件执行一次，算完成一轮循环。
- 用户可以设置循环次数，完成后整个任务结束。
- 用户可以预先配置并命名循环方案，保存多套方案，后续使用时直接选择。
- 保存计时历史，记录使用日期、使用的循环方案和实际计时时长。
- 事件结束并切换到下一事件时，使用提示音和震动提醒，屏幕同步显示新事件及其剩余时间。
- 前台计时时屏幕保持常亮；用户手动锁屏或切到其他应用后，仍继续计时和提醒。
- 点击开始后默认先准备 5 秒，可在每套方案中调整或关闭；准备只在整次开始前执行一次，不随循环重复。
- 历史计时时长累计所有事件的实际运行时间，包含休息事件，排除开始前准备和暂停。
- 中途结束或重置时，保留实际计时时长和已完成轮数，标注“提前结束”；仅在准备阶段取消则不生成正式历史记录。
- 选择已保存方案后，可临时调整本次循环次数；原方案不变，历史记录本次实际使用的设置。
- 第一版供自己或少数人直接安装 APK 使用，之后再考虑公开发布。
- 第一版的循环方案和历史记录只在当前手机本地保存，暂不提供备份导出、导入恢复或云同步。
- 主要使用设备为小米 15，将其作为第一版重点适配和验收的机型；具体系统版本在真机测试前确认。
- 计时页面以大数字为主，突出当前事件和倒计时，下方显示当前轮次与下一事件。

## 3. 建议的第一版规则

以下是为了让第一版完整可用而提出的默认规则，尚不代表用户已经逐项确认。

- 采用倒计时，时长以秒为最小单位，通过“分、秒”输入。
- 至少配置一个事件；名称不能为空；单个事件时长必须大于 0；循环次数必须为正整数。
- 允许不同位置的事件使用相同名称，例如一轮内安排多次“休息”。
- 支持添加、编辑、删除和调整事件顺序；顺序就是执行顺序。
- 循环次数包含第一轮。例如设置 3 次，就是总共执行 3 轮。
- 事件之间和轮次之间直接切换。每轮需要重复的热身或休息作为普通事件加入列表；仅执行一次的开始前准备独立配置。
- 支持开始、暂停、继续和重置。暂停期间所有计时停止，继续时接着剩余时间执行。
- 运行期间使用启动时的配置；修改方案前先结束或重置当前计时，避免中途改变执行顺序。
- 事件切换默认采用已确认的提示音＋震动，同时更新屏幕内容；建议提供声音与震动的独立开关，并为全部完成设置不同的提醒。
- 常亮在前台准备和事件计时运行期间启用；暂停、完成或离开计时页后恢复系统的自动息屏规则，不主动解除用户的手动锁屏。
- 每套循环方案保存名称、完整事件列表及顺序、各事件时长、循环次数、开始前准备时长和提醒设置。
- 方案名称去除首尾空格后不能为空，例如“晨间训练”“拉伸放松”；与事件名称分开填写。
- 所有已保存方案保存在手机本地，重新打开后仍能选择；记住上次选择的方案，但不自动开始计时。
- 历史记录按已确认要求保存在手机本地，关闭并重新打开应用后仍可查看。
- 第一版离线使用，不依赖账号或服务器，以原生安卓应用为实现方向；备份导出、导入恢复和云同步不属于本版范围。

### 循环方案的建议规则

- 首次打开且没有方案时，进入创建方案的配置页；保存第一套方案后，可立即开始或返回“我的方案”。
- 已有方案时，默认进入“我的方案”，选择一套并点击开始即可使用，也可以继续创建其他方案。
- 每套方案独立保存；新建不会覆盖已有方案。编辑只在点击保存后生效，取消编辑保留原配置。
- 允许重命名、修改事件与时长、修改循环次数，以及删除不再使用的方案。
- 不同方案使用独立标识，即使名称相同也不会互相覆盖；列表同时显示事件摘要、轮数和总时长，便于区分。
- 删除方案需要确认，只移除可选方案，不删除该方案过去的计时历史。
- 删除所有方案后显示创建入口，历史记录仍可正常查看。
- 同时只允许一套方案计时；已有准备中、运行中、暂停或待恢复的任务时，重新打开应用优先展示该任务。结束或重置后才能启动另一套方案。

### 本次循环次数的规则

- 已确认：选择方案后，默认使用方案保存的循环次数；用户可以在开始前临时修改，只影响这一次计时。
- 本次循环次数必须为正整数；修改时立即更新本次事件计时总时长及包含准备的预计总时长。
- 临时调整不写回方案库。修改事件、时长或方案默认轮数，仍通过方案编辑并保存完成。
- 建议取消选择、切换方案或结束后重新从列表选择时，重新读取方案默认轮数，避免上一次临时调整影响下次使用；已有任务的恢复仍使用该任务原本的实际轮数。
- 计时开始后固定本次轮数，暂停或恢复不改变它；在计时页重置后重新开始、或完成页“再来一次”，都沿用本次配置，包括临时轮数，但会生成新的计时会话。返回列表重新选择时才恢复方案默认轮数。
- 历史中的计划轮数、总时长、完成轮数和完成状态，都以本次实际配置为准，不以方案原先的默认轮数判断。

### 开始前准备的规则

- 已确认默认 5 秒，配置为 0 秒表示关闭，其他值为非负整数秒。
- 准备时显示“准备”及剩余秒数，结束后进入第 1 轮、第 1 个事件，并使用提示音＋震动提示事件开始。
- 准备不占用事件位置或循环轮数；后续轮次之间不重复准备，“再来一次”则重新执行一次准备。
- 建议准备期间支持暂停和继续，继续时接着剩余准备时间运行；已确认仅在准备阶段取消不生成正式计时历史。
- 界面分别显示事件计时总时长和额外准备时长，预计完成所需时间包含这一次准备。

### 历史记录的建议规则

- 一次从开始到结束的计时会话对应一条历史记录；其中执行多轮仍只记录一次。
- 记录开始日期与时间、结束时间、方案名称、当时的事件及其顺序和时长、本次实际设置的计划循环次数、已完成轮数、实际计时时长和结束状态。
- 已确认：“实际计时时长”累计各事件的实际运行时间，排除开始前准备和暂停；方案里的“休息”事件属于正常计时，因此计入时长。完整完成时等于计划的事件计时总时长，提前结束时包含当前未完成事件已经计时的部分。
- 已确认：正常结束标记为“已完成”；用户提前结束或确认重置时，保留实际计时时长及完整完成的轮数，并标记为“提前结束”。仅在准备阶段取消不生成正式历史记录；尚未产生有效事件计时时长的立即重置同样不生成正式记录。
- 暂停、继续和从后台返回都沿用同一次记录，不新增记录；“再来一次”则建立新记录。
- 按本次开始时的本地日期归档，同时保存当时的时区信息。跨午夜的一次计时保留一条记录，在详情展示完整起止日期与时间。
- 历史保存启动时本次实际配置的副本，包括临时设置的轮数；之后修改方案名称、事件或默认轮数，或者删除方案，都不改变旧记录。
- 系统中断后提供恢复入口，只保留可可靠恢复的计时进度，中断等待时间不计入。已有有效事件计时的会话在历史中展示“待恢复”，继续时沿用原记录，选择结束或重置则标记为“已中断”。如果仅在准备阶段中断、没有有效事件计时，只保留恢复草稿；取消或重置时丢弃草稿，不生成正式历史。

## 4. 页面规划

### 我的方案页（首页）

- 展示所有已保存的循环方案，每项显示名称、事件摘要、事件数、循环次数和预计总时长。
- 选择方案后可以直接点击开始，启动前无需重新配置。
- 选中方案后提供“本次循环次数”输入，默认带入方案轮数，允许临时调整；同时更新本次时长预览，并说明“仅本次生效”。不调整时仍可直接开始。
- 提供新建、编辑和删除方案的入口。
- 提供历史记录入口；没有方案时仍可查看历史。
- 示例：“晨间训练 · 运动 → 休息 · 2 个事件 · 默认 3 轮 · 计时 4 分钟＋准备 5 秒”。

### 方案配置页（新建／编辑）

- 设置整组循环的方案名称。
- 展示按顺序排列的事件列表，每项显示名称和时长。
- 提供添加、编辑、删除、上移和下移操作。
- 设置循环次数。
- 设置开始前准备时长，默认 5 秒，可调整或关闭。
- 实时显示单轮时长、事件计时总时长和包含准备的预计总时长。
- 配置无效时在相应字段说明原因；配置有效后可选择“保存”或“保存并开始”。
- 保存后方案出现在“我的方案”，下次直接选择使用；取消编辑不修改已保存内容。

### 计时页

- 采用已确认的大数字布局：当前事件名称和倒计时占据主要区域，下方显示轮次及下一事件；整套事件列表在方案页查看。
- 显示本次使用的循环方案名称。
- 开始前先显示准备倒计时，完成后切换为当前事件；关闭准备时直接进入事件计时。
- 用大字显示当前事件名称和剩余时间。
- 显示当前轮次，例如“第 2 / 3 轮”，总轮数使用本次实际设置。
- 在次要区域显示当前事件位置，例如“事件 2 / 3”，以及下一个事件；处于本轮最后一个事件且还有下一轮时，提示下一轮的首事件；整次最后一个事件时显示“接下来完成”。
- 将整次任务剩余时间作为次要信息，保持当前事件倒计时最醒目。
- 事件切换时同时发出提示音和震动，并将主显示更新为新事件名称与剩余时间。
- 前台运行时保持屏幕常亮；用户手动锁屏后无需亮屏也能继续计时和接收提醒。
- 提供暂停、继续、结束本次和重置操作，按钮方便运动时点击。
- 运行中的重置操作需要确认，避免误触丢失当前进度。

### 完成状态

- 明确显示全部完成和已完成轮数。
- 展示本次实际计时时长，并提供查看本次历史记录的入口。
- 可以按本次配置再来一次，或返回“我的方案”选择其他方案。

### 历史记录页

- 按开始日期分组，较新的记录排在前面。
- 每条记录展示开始时间、方案名称、实际计时时长和状态。
- 提前结束的记录与完整完成的记录一起展示，标注“提前结束”和已完成轮数；不足一整轮时显示已完成 0 轮，已计时时长仍保留。
- 点击记录查看当时的事件配置、计划轮数、已完成轮数及完整起止时间。
- 没有记录时显示清晰的空状态。
- 示例：“2026 年 9 月 7 日 08:00 · 晨间训练 · 计时 4 分钟 · 已完成 3 / 3 轮”。

## 5. 核心逻辑与安卓运行方式

### 数据与状态

- 事件：唯一标识、名称、时长；列表位置决定顺序。
- 计时方案：方案唯一标识、方案名称、有序事件列表、默认循环次数、准备时长、提醒设置、创建及更新时间。
- 本次运行配置：来源方案标识、方案名称、事件与准备设置副本、本次循环次数；由所选方案和可选的临时轮数组成，启动时固定，独立于方案库保存。
- 运行状态：未开始、准备中、运行中、已暂停、待恢复、已完成、已结束。
- 当前进度：当前阶段（准备或事件）、当前事件、当前轮次、阶段剩余时间、整次任务剩余时间；暂停和恢复时保留原阶段。
- 计时记录：会话唯一标识、开始及结束时间、开始日期及时区、来源方案标识、本次运行配置副本、计划事件计时总时长、实际计时时长、已完成轮数、记录状态；开始时间按点击开始记录，实际计时时长独立累计。
- 方案库、运行进度和历史记录分别管理，页面展示与计时逻辑分开实现。
- 方案和历史使用应用本地持久化存储；新建、保存、计时和查看历史均不依赖网络，不增加账号或云端存储流程。

### 方案保存与选择

- 本地保存多套方案，通过方案标识进行读取、更新和删除，不以名称判断是否覆盖。
- 一次保存完整写入名称、事件顺序、时长、循环次数和准备设置，保存成功后才更新列表或开始计时。
- 从方案列表开始时，读取所选方案，应用可选的本次轮数并固定为运行配置副本；该操作不更新方案库。
- 历史详情直接使用保存的本次运行配置副本，删除方案不连带删除历史，也不会导致历史详情无法打开。

### 历史保存

- 开始时生成本次会话的唯一标识，并持久化本次运行配置副本和初始运行状态。
- 暂停、阶段切换及运行检查点保存已累计时长，保证重新进入后可以恢复。
- 结束时将实际时长、已完成轮数和结束状态一起写入本地记录。
- 实际时长只累计事件阶段的运行时间，准备与暂停单独处理；不能直接用结束时间减开始时间作为历史计时时长。
- 用户结束或重置时，只要已有有效事件计时就保存“提前结束”记录；仅取消准备阶段则丢弃会话草稿，不生成正式历史。
- 所有保存操作以会话标识更新同一条记录，重复完成回调或页面重建不会产生重复历史。
- 正常运行中的会话在计时页显示；产生有效事件计时后结束才进入正式历史。异常中断且已有事件计时的会话在历史中显示“待恢复”，仅准备阶段中断的草稿通过计时页恢复。

### 时间推进

1. 开始时固定本次配置；准备时长大于 0 则先进入准备阶段，结束后从第 1 轮、第 1 个事件运行，否则直接开始事件。
2. 当前事件归零时进入下一事件。
3. 本轮最后一个事件结束时，按本次运行配置的循环次数判断：未完成则进入下一轮，已完成则停止。
4. 暂停时保存剩余时长；继续时根据剩余时长重新确定截止时点。
5. 使用单调时间计算实际经过时长，避免仅靠界面每秒减一造成误差，也避免修改系统时间影响当前计时。
6. 页面刷新或回调延迟后，按实际经过时间推进到正确事件和轮次；跨过多个边界时，也必须正确处理整次完成。
7. 事件计时总时长等于所有事件时长之和乘以本次循环次数；预计整次用时再加一次准备时长，计算时检查数值范围。

### 安卓生命周期

- 以小米 15 的实际系统环境为重点验证对象，测试前记录设备系统版本，核对锁屏、后台计时、提醒和通知行为。
- 计时逻辑不依赖页面是否可见，旋转屏幕和页面重建不应重置计时。
- 按用户确认的方式，在前台计时运行时保持屏幕常亮，手动锁屏或切到其他应用不触发暂停。
- 通过适合持续计时的前台服务和状态通知，支持用户切换应用或锁屏后的运行及提醒。
- 在开发时核对目标安卓版本的服务、通知及权限要求。
- 应用重新进入时读取当前计时状态，展示正确进度。
- 保存恢复所需的运行信息；若计时服务已中断，重新打开时展示恢复状态，由用户选择继续或重置。

## 6. 实施步骤与交付物

第一版交付方式已确认：提供可直接安装的 APK 供个人或小范围使用。应用商店上架不属于本次交付；后续再决定公开发布的安排。

### 第一步：建立项目与页面骨架

- 检查本机安卓开发、构建和模拟器环境。
- 确定最低安卓支持版本、原生开发工具和项目结构。
- 将小米 15 列为重点真机测试设备，记录实际系统版本后确定相应的兼容性检查项。
- 建立“我的方案”、方案配置页、计时页、完成状态和历史记录页的基础导航。

交付物：可以安装并打开的安卓应用骨架。

### 第二步：实现方案配置、保存与选择

- 完成事件的增删改和顺序调整。
- 完成分秒输入、循环次数、准备时长输入和校验。
- 完成方案名称输入和单轮、总时长预览。
- 实现多套方案的本地保存、新建、编辑及删除。
- 实现首次创建引导、方案列表和选择后直接开始的流程。
- 实现开始前临时调整本次循环次数和时长预览，不覆盖方案默认设置。
- 确保取消编辑不覆盖原方案，删除方案不删除历史。

交付物：用户可以创建并命名多套循环方案，重新打开应用后直接选择使用，也可临时调整本次轮数。

### 第三步：实现计时引擎

- 实现事件推进、轮次推进和全部完成判断。
- 实现仅在整次开始前执行一次的准备阶段，以及准备期间的暂停和恢复。
- 实现暂停、继续、结束本次和重置，按已确认的口径累计事件运行时间，排除准备和暂停。
- 接入计时页，按本次配置显示当前事件、轮次和剩余时间，恢复时保留本次轮数。
- 使用可控制的时间源验证边界，避免测试必须等待真实的长倒计时。

交付物：前台完整执行指定方案，轮数与总时长正确。

### 第四步：完善手机使用体验

- 按已确认的方式实现事件切换提示音、震动及屏幕更新，再加入可区分的完成提醒。
- 接入后台计时、通知和状态恢复，实现前台常亮及手动锁屏后继续运行的行为。
- 按已确认布局突出当前事件与大数字倒计时，下方放置轮次和下一事件；完善按钮尺寸、小屏布局和返回操作。

交付物：可以在运动场景中使用，并完成锁屏、后台和恢复行为验证。

### 第五步：实现历史记录

- 建立本地计时记录存储，保存会话标识、本次运行配置副本、日期、实际时长和状态。
- 接入正常完成、提前结束、重置与中断恢复时的记录更新，按已确认规则保留提前结束记录、排除仅准备后取消的记录。
- 实现按日期分组的历史列表和单次记录详情。
- 验证方案修改或删除不会影响旧记录，同一次计时不会重复保存。
- 验证临时调整轮数后的历史记录使用本次配置，完成判断不受原方案默认轮数影响。

交付物：用户能查看自己在哪天使用哪个循环方案进行了多久的计时。

### 第六步：验收与打包

- 完成下列逻辑测试和安卓设备验证。
- 在小米 15 上重点验证屏幕常亮、手动锁屏、切换应用后的计时与声音震动提醒，以及方案和历史的本地保存。
- 修复事件边界、轮次边界和生命周期问题。
- 验证历史记录的时长、日期归档、持久化及恢复行为。
- 验证断网情况下仍可创建和选择方案、完成计时并保存和查看历史。
- 生成可安装的 APK，附安装说明和简短使用说明。
- 使用稳定的应用标识和签名方式生成小范围分发安装包，保留后续覆盖安装升级所需的签名材料；验证同一应用升级后方案与历史仍保留。

交付物：第一版可安装 APK、源代码、安装与使用说明，以及后续版本打包和升级的必要说明。

## 7. 验收标准

| 场景 | 预期结果 |
| --- | --- |
| 首次使用且没有方案 | 进入创建流程，配置名称、事件和循环次数后可以保存 |
| 创建“晨间训练”和“拉伸放松”两套方案后重启应用 | 两套方案均可选择，各自名称、事件、时长和轮数保持一致 |
| 从方案列表选择一套并开始 | 按该方案已保存的配置运行，无需再次填写配置 |
| 方案默认 5 轮，开始前临时改为 3 轮 | 本次只执行 3 轮，时长预览随之更新，方案默认值仍为 5 轮 |
| 上述临时 3 轮全部完成 | 历史显示计划 3 轮、完成 3 / 3 轮和“已完成”，时长按 3 轮计算 |
| 临时修改轮数后取消选择，再选择该方案 | 恢复方案默认轮数，不残留临时值 |
| 使用临时 3 轮启动后锁屏或重新进入应用 | 原任务继续使用 3 轮，不恢复为方案默认 5 轮 |
| 临时 3 轮完成后点击“再来一次” | 新会话仍使用 3 轮并执行一次准备，原方案仍为 5 轮 |
| 本次循环次数输入 0、负数或非整数 | 提示输入有效轮数，不允许启动，也不修改原方案 |
| 编辑一套方案并保存 | 仅更新该方案，其他方案保持不变 |
| 修改配置后取消编辑 | 已保存方案保持原内容 |
| 方案名称为空或只有空格 | 提示填写方案名称，不能保存或开始 |
| 创建两套同名方案 | 独立保存，不互相覆盖，列表展示各自的配置摘要 |
| 删除曾经使用的方案 | 方案从列表移除，已有历史及详情完整保留 |
| 删除所有方案后重新进入 | 可以创建新方案，仍能查看历史 |
| 已有准备中、运行中、暂停或待恢复的任务时重新打开 | 优先展示原任务，不自动开始另一个方案 |
| 配置多个不同名称和时长的事件 | 保存后名称、时长和顺序一致 |
| 3 个事件，循环 2 次 | 依次执行 A → B → C → A → B → C，随后停止 |
| 1 个事件，循环 1 次 | 完成该事件后停止 |
| 1 个事件，循环多次 | 正确累计轮次，到达设定次数后停止 |
| 示例方案循环 3 次，准备 5 秒 | 事件计时 5 分 30 秒，预计总用时 5 分 35 秒，完成后不会多执行一轮 |
| 设置准备 5 秒，循环 3 次 | 开始前准备一次，后续轮次直接衔接，不再次准备 |
| 关闭准备时间后开始 | 直接进入第 1 轮、第 1 个事件 |
| 准备倒计时中暂停后继续 | 接着剩余准备时间运行，不提前开始事件 |
| 准备期间取消，或完成后选择“再来一次” | 取消不生成正式计时历史；再来一次重新执行准备 |
| 空名称、零时长或无效循环次数 | 指明输入问题，不能启动无效方案 |
| 暂停后等待再继续 | 暂停时长不计入事件耗时，剩余时间不丢失 |
| 在事件或轮次边界暂停、继续 | 不跳过事件，不重复计算轮次 |
| 界面刷新延迟，跨过多个事件 | 根据实际经过时间进入正确状态 |
| 准备 5 秒，首事件 30 秒，首次刷新延迟到开始后的第 8 秒 | 已进入首事件，剩余 27 秒，历史累计事件计时为 3 秒，准备时间只扣除一次 |
| 旋转屏幕、锁屏、切换应用再返回 | 当前事件和轮次正确，计时不会从头开始 |
| 前台计时超过系统自动息屏时间 | 屏幕保持常亮，暂停或结束后恢复系统息屏规则 |
| 手动锁屏或切到其他应用后到达事件边界 | 计时继续推进，并按配置发出提示音和震动 |
| 连续点击开始、暂停或继续 | 不产生多个同时运行的计时任务 |
| 本次临时设为 3 轮，计时页重置后重新开始 | 原会话按实际进度处理历史；新会话仍用本次 3 轮，重新准备后从第 1 轮、第 1 个事件开始 |
| 全部结束后 | 仅触发一次完成提醒，不再进入后续事件 |
| 当前事件结束并进入下一事件 | 发出一次提示音和震动，屏幕显示正确的新事件与剩余时间 |
| “运动 60 秒 → 休息 20 秒”循环 3 次并完成 | 保存一条历史：对应方案、开始日期、实际计时 4 分钟、完成 3 / 3 轮 |
| 上述方案关闭准备，中途暂停 1 分钟再完成 | 实际计时仍为 4 分钟，起止时间跨度为 5 分钟 |
| 准备 5 秒，运动 30 秒、休息 10 秒，循环 1 轮，另暂停 20 秒后完成 | 历史实际计时 40 秒，起止跨度 65 秒，准备和暂停不计入实际计时 |
| 运动 60 秒、休息 20 秒，循环 3 轮，准备结束后累计事件计时 90 秒即提前结束 | 保存实际计时 1 分 30 秒、完成 1 / 3 轮和“提前结束”状态 |
| 每轮 80 秒，第一轮仅运行 30 秒就结束 | 保存实际计时 30 秒、已完成 0 轮和“提前结束”状态 |
| 运行一段时间后确认重置 | 保留本次实际计时记录；再次开始生成另一条记录 |
| 暂停、继续、旋转屏幕或重复收到完成回调 | 同一次计时最多对应一条历史记录 |
| 修改当前方案名称、事件时长或轮数 | 旧历史仍显示使用时的配置 |
| 23:59 开始，次日结束 | 按开始日期归档一条记录，详情展示跨日的起止时间 |
| 关闭并重新打开应用 | 已保存历史仍可查看，实际时长与状态保持一致 |
| 手机断网后创建方案、计时并重新打开应用 | 本地方案与历史均可正常保存和读取，核心功能不要求登录或联网 |
| 系统中断后恢复或结束 | 沿用原会话记录，中断等待不计入，结束时状态正确 |
| 仅准备阶段被中断，恢复入口选择结束或重置 | 丢弃无事件计时的草稿，不生成正式历史记录 |
| 用相同应用标识及签名覆盖安装后续版本 | 已保存方案和历史记录保留，应用可以正常打开和使用 |
| 在小米 15 上执行实际使用流程 | 可安装运行，计时页面显示正常，常亮、锁屏及后台提醒、本地方案和历史符合上述标准，并记录测试时的系统版本 |
| 打开计时页或切换事件 | 当前事件及倒计时最醒目，下方轮次和下一事件正确，长事件名称不遮挡倒计时和操作按钮 |

计时逻辑、方案保存与选择，以及历史保存、去重和时长计算采用自动化测试；首次创建、方案切换、声音、震动、通知、锁屏、后台和历史页面行为需要在模拟器与实际安卓手机上验证。

## 8. 后续可选扩展

第一版完成后，再根据实际使用决定是否增加预设模板、语音播报、无限循环和历史统计图表，以及是否公开发布到应用商店。

第一版代码与签名 APK 已完成：方案配置和保存、循环计时、临时轮数、提醒、暂停和恢复、本地历史及页面已实现。旧版已在小米 15 上成功安装，并通过 11 项 SQLite 存储与恢复测试；长时间锁屏和提醒尚待完整验收。使用与构建说明见 README.md，验证结果及手机验收步骤见 TESTING.md。

## 9. 1.1.0 新增需求（2026-09-09）

- 应用名称改为 TempusAct，保留原应用标识、签名及本地数据，支持从健身钟覆盖升级。
- 历史记录支持单条和多选删除；删除前确认，删除记录不删除循环方案，也不影响其他计时会话。
- 按用户确认的方式，按使用的循环方案自动分组；按方案唯一标识判断归属，同名的不同方案独立成组。
- 每组显示实际总用时及记录数；累加已结束记录的实际事件计时，包含休息与提前结束部分，排除准备、暂停和中断等待。
- 同一方案改名后仍归为一组，组名显示最近一次使用时的名称；历史详情保留各次原始快照。
- 删除记录后重新计算组总用时；删除一个方案不影响已有历史分组；没有历史记录的方案不显示为空组。
- 待恢复记录仍可查看，结束或继续完成后可删除；它不计入已结束记录的总用时。
- 数据库无损升级，并持久化删除标识，防止旧的完成回调让已删除记录重新出现。
- 修复编辑页面键盘弹出时重复保留底部空间的问题。

1.1.0 已实现并生成签名 APK：58 项单元测试通过，静态检查无问题，签名与 ZIP 对齐验证通过。已覆盖安装到小米 15，17 项安卓仪器测试全部通过。升级后已核对原有方案与历史保留；历史按方案分组，删除后记录数和总用时同步更新，方案仍保留。键盘布局修复已通过真机复查。长时间锁屏及声音、震动的完整验收仍见 TESTING.md。
